__pycache__/
*.pyc

# local config: copy config.example.yaml -> config.yaml
config.yaml

# runtime data (SQLite file / MySQL volume target)
# 模板进版本库，方便直接 cp data/config.example.yaml data/config.yaml
data/*
!data/.gitkeep
!data/config.example.yaml

# local virtualenv
.venv/

# tool scratch
.omo/
.pytest_cache/

# compose 本机密钥注入（从 .env.example 复制）
.env
